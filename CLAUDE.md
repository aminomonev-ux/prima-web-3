# PRIMA — RSJD Dr. Amino Gondohutomo | Next.js Web App

> ⚠️ **WAJIB DIIKUTI SELURUHNYA. Tidak ada pengecualian.**

---

## 🛡️ AUDIT-AWARE DEVELOPMENT

> 📖 **Cheatsheet-nya: `docs/AUDIT-LESSONS-LEARNED.md`** — L1–L70 lengkap + status tiap pelajaran di lingkungan intranet + rangkuman 4 putaran audit BLUD. Baca itu dulu sebelum menyentuh kode. Berkasnya **hanya hidup di komputer lokal** (`.gitignore` → `/docs/AUDIT-*.md`, karena repo ini publik dan isinya temuan keamanan); kalau tidak ada di workspace Anda, mintakan salinannya — jangan di-commit.
>
> ⚠️ **Catatan prima-web-3**: folder dokumentasi audit asli (`docs/audit/`, `docs/security-audit/`, `docs/audit-v3/`, `docs/audit-V5/`) **tidak ikut ter-copas** ke workspace ini — yang ada di sini versi ringkasnya. Kalau butuh narasi panjang + potongan kode sebelum/sesudah tiap temuan, sumbernya di repo `prima-web` asli.

PRIMA punya 102 findings audit dari multiple wave (Audit v1.0, Tahap 11/12/14/15, SDL-Audit v1.1, BLUD v1.2, Audit V3 V3-1..V3-6). Ringkasan anti-pattern kritis di tabel bawah.

### 🔴 WAJIB DIPATUHI (highlight 6 pattern paling kritis):

| ❌ JANGAN | ✅ HARUS | Ref |
|---|---|---|
| `process.env.JWT_SECRET \|\| 'fallback'` | Throw kalau env kosong | SEC-C1 |
| `.toISOString()` ke kolom DATETIME MySQL | `toMysqlDatetime(date)` helper | MySQL 8.4 |
| `for (...) await sql\`INSERT...\`` loop | `bulkInsert(table, cols, rows)` | PERF-C1 |
| `DELETE; for INSERT;` tanpa transaksi | `withTransaction(async ({tx}) => {...})` | BUG-C2 |
| `UPDATE WHERE id = ?` tanpa ownership | Tambah `AND recipient/user_id IN (...)` | SEC-C4 IDOR |
| Simpan `*_token` plaintext di DB | `SHA256(rawToken)` at-rest, raw di email URL | **SDL-M18 / SEC-C5 / L45** |
| Set `x-user-*` di `response.headers` (spoofable) | Strip header klien + set di **request** header proxy | **V3-1 / L54** |
| Counter baca-lalu-tulis di JS (`x = col+1` → `UPDATE SET col=${x}`) | Atomik `UPDATE SET col = col + 1` (lost-update) | **V3-5 / L55** |
| Sakelar maintenance cuma bikin kartu /menu abu | Cek flag di **halaman DAN tiap route API** | **T1 / L72** |
| Kode konfirmasi dikirim klien **beserta** kunci jawabannya | Konstanta milik server (`z.literal`) | **T4 / L73** |
| `res.affectedRows` langsung pada hasil `sql` (itu **array**) | `execWrite(sql\`…\`)` atau `res[0].affectedRows` | **T15 / L74** |
| `expected_version` diambil dari versi yang **dibuka**, padahal yang ditulis versi **lain** | Angka kunci harus milik kunci TUJUAN (`expectedVersionUntuk`) | **L75** |
| Layar hapus menebalkan **stempel waktu**, identitas datanya jadi subtitle; gesekan konfirmasi berupa **kode acak** | Identitas jadi kepala grup; yang diketik = **sasarannya** (tahun anggaran) | **L76** |
| Memuat snapshot lama lalu memakai angka kunci **milik snapshot** sebagai `expected_version` | Angka kunci selalu diambil **segar dari server** — L75 lewat pintu lain | **L77** |
| Modal (impor/salin) punya **tanggal versi + jalur tulis sendiri** di samping tombol Simpan halaman | Modal berhenti di **form** (`onTerapkan`); yang menulis SATU tombol, satu tanggal | **L78** |
| Pemilih periode cuma memindahkan **target** simpan, layar tidak ikut berganti | Ganti periode = ganti konteks: baris, versi, angka kunci ikut disetel | **L78b** |
| Sesudah Simpan berhasil, pemilih periode dipulangkan ke bulan berjalan (`setPeriodeTulis('')`) | Periode **tetap** di versi yang barusan ditulis — koreksi kedua wajib mendarat di bulan yang sama | **L79** |
| Satu bendera (`versi`) menjawab **dua** pertanyaan: "slot mana yang dibuka" DAN "sudah tersimpan belum" | Dua state: `versi` + `belumTersimpan` (Pulihkan mengisi `versi` tapi isinya belum tersimpan) | **L79b** |
| Tombol yang **mengganti seluruh tabel** (Form Baru/Impor/Buat Pergeseran) hidup terus di atas versi tersimpan | Dikunci selama versi tersimpan terbuka; Salin Tahun patokannya **TAHUN**, bukan slot versi | **L79c** |
| Pemilih periode hanya menawarkan bulan **kosong**, jadi bulan yang sudah berarsip cuma bisa dibuka lewat daftar versi | Bulan berarsip ikut ditawarkan (`tanggalPeriodeHistoris`) dan memilihnya **memuat** arsipnya | **L79d** |
| Modal yang mengambil baris dari tempat lain ikut **memindahkan sasaran Simpan** | Menyalin mengganti **ISI**, tidak pernah sasaran; sasaran ditampilkan, bukan dipilih | **L80** |
| `partialRecalc*` membuka dengan `rows.map(r => ({ ...r }))` — 558 baris dapat identitas baru tiap ketikan | Hanya baris yang angkanya **bergeser** yang dikloning; larik luar tetap baru | **L81** |
| Baris tabel menerima `selectedRowIds`/`highlightId`/`childCount` mentah | Dioper sebagai **boolean** per baris, kalau tidak `React.memo` cuma hiasan | **L81b** |
| Tombol dimatikan di layar dianggap = jalurnya aman; API-nya dibiarkan salah | Pagar di **API**; tombol mati cuma menyembunyikan, tidak memperbaiki | **L82** |
| Sinyal turunan (`vol_p === vol` = "belum digeser") dipercaya selamanya | Fitur baru bisa membuat premisnya **tidak berlaku lagi** — cek pemakainya | **L82b** |
| Uji mutasi memakai kutipan **sepotong** (`/asalTutup && existing > 0/`) | Kutip **utuh sampai kurung buka** — `false &&` di depannya tetap cocok | **L82c** |
| Label yang menyebut identitas data **tersimpan** (pil versi + jumlah baris) dibiarkan diam saat sebuah aksi mengganti isi layar | Label itu wajib menyatakan isinya sudah bergeser (`belumTersimpan`) — kalau tidak, ia berbunyi "arsipnya ikut tertimpa" | **L83** |
| Pertanyaan berlingkup **setahun** (`MAX(versi_tanggal)` = siapa sumber pagu) dijawab **sebelum** kunci dipegang | Kunci setahun DULU, sebagai perintah **pertama** transaksi — snapshot lahir di `SELECT` biasa pertama | **L84** |

Anti-pattern terbaru (2026-06-15): **L66** `sqlInt()` utk `LIMIT`/`OFFSET` (mysql2 tolak `LIMIT ?` — lolos tsc, ketahuan saat live), **L67** cap jumlah sheet exceljs anti zip-bomb (di parser bersama), **L68** Zod + cek-eksistensi soft-FK di SEMUA cabang tulis termasuk tabel peta/lookup.

**L69** (2026-08-01, audit BLUD N1–N4): perbaikan itu belum selesai sampai SEMUA jalur tulis kena. Tiga dari empat temuan putaran 4 adalah sisa perbaikan lama — `FOR UPDATE` dipasang tapi 1 fungsi terlewat, nomor urut diberikan di `create` tapi tidak di `update`, kill-switch dipasang di API tapi tidak di layar. Tiga turunannya: (a) `SELECT … FOR UPDATE` pada baris yang **belum ada** tidak mengunci apa pun — `INSERT IGNORE` dulu (pola `acquireBludLock`); (b) fungsi baca yang dipanggil dari dalam transaksi WAJIB menerima `tx` (tipe `Penanya` di `db.ts`), kalau tidak ia diam-diam memakai koneksi lain dan kunci di atasnya sia-sia; (c) aturan yang jangkauannya **setahun** butuh kunci setahun (`BLUD_PERIODE_ENTITY`) — mengunci baris bulannya sendiri tidak menjaganya.

## 🚨 WAJIB — Navigasi Codebase (lean-ctx)
**DILARANG** membaca file sembarangan atau grep membabi buta. Gunakan tool lean-ctx.

> ℹ️ **Catatan prima-web-3**: `graphify-out/` (knowledge graph) tidak ter-copas ke workspace ini, jadi langkah "baca GRAPH_REPORT.md dulu" & command `/graphify` di-skip. Kalau mau graph, jalankan `/graphify .` untuk generate ulang.

### Tool mapping (TIDAK BOLEH dilanggar):
| ❌ DILARANG | ✅ WAJIB PAKAI |
|------------|---------------|
| `Read` / `cat` | `ctx_read(path, mode)` |
| `Grep` / `rg` | `ctx_search(pattern, path)` |
| `Bash` / `Shell` | `ctx_shell(command)` |
| `ls` / `find` | `ctx_tree(path, depth)` |
| Edit butuh Read? | `ctx_edit(path, old, new)` |

---

## 🚨 WAJIB — Aturan Edit

- Surgical: edit HANYA bagian bermasalah, jangan sentuh yang benar dan ketika memperbaiki/menambah, fungsi lain yang berhubungan juga disesuaikan
- Selalu cek `docs/schema-mysql.sql` sebelum menyebut kolom/tabel
- Setiap kolom/tabel baru: **WAJIB** tambahkan ke `docs/schema-mysql.sql` + buat `docs/migrations/migration-*.sql` (MySQL syntax, tanpa `IF NOT EXISTS` pada `ADD COLUMN`)
- Client fetch: pisah `fetch()` dan `.json()`, bungkus try/catch
- **Multi-statement write WAJIB `withTransaction`** (CQ-01/V5): setiap INSERT/UPDATE/DELETE >1 statement yang harus all-or-nothing dibungkus `withTransaction({tx,conn})` + `bulkInsert`; recompute turunan (mis. `updateHeaderStats`) setelah commit. Jangan loop `await sql` lepas.
- **Modul kolaboratif tanpa ownership per-record** (AUTHZ-02/V5): BLUD/Kinerja/LKJIP/BBA/Rencana-Aksi/IKI sengaja akses berbasis role+`app_access` (semua pemegang akses bisa edit semua record) — BUKAN IDOR. Konsekuensinya: pemberian `app_access` harus konservatif & direview berkala. Modul yang butuh batasan sub-bidang (mis. unduh file Usulan) menerapkan scoping eksplisit (lihat `upload/download/route.ts`).

## 🚨 WAJIB — Comment Style (Minimal Comments)

**Rely on self-explanatory naming, BUKAN komentar.** Function name + variable name harus cukup jelas tanpa komentar tambahan.

| ✅ Boleh komentar | ❌ JANGAN komentar |
|---|---|
| **WHY**: decision, anti-pattern reference, edge case, workaround | **WHAT**: ngulang code yang sudah self-explanatory |
| Pointer ke audit lesson (e.g. `// L4: withTransaction wajib`) | `// Set name to John` di atas `name = 'John'` |
| Browser/library bug workaround | Komentar dekoratif tanpa info |
| Non-obvious algorithm + reference | Section divider `// ─────` tanpa konteks |
| TODO/FIXME dengan context spesifik | Komentar yang outdated/tidak match code |

Header file/section comment tetap OK untuk navigasi (1-2 baris ringkas). Hindari long-form explanation di code — kalau perlu doc panjang, taruh di `docs/*.md`.

## 🚨 WAJIB — Response
1. 🔴 WAJIB Tool dulu, penjelasan sangat ringkas di akhir. (Ringkas: file + perubahan, narasi sangat singkat to the point.)
2. 🔴 **"tanya saja"** → jawab tanpa rubah code (zero tool call kecuali read untuk verify). Jangan eksekusi sampai user explicit minta.
3. 🔴 **"jawab singkat"** → balasan 1-3 kalimat max. Tanpa table, tanpa heading, tanpa narasi panjang. To the point only.
4. 🔴 **Bahasa humanis, mudah dimengerti, singkat tapi detail.** Bicara seperti rekan kerja yang menjelaskan ke sesama manusia — bukan laporan mesin.
   - **Singkat tapi detail** = padat, bukan dangkal. Buang basa-basi ("Baik, saya akan…", "Semoga membantu"), pengulangan permintaan user, dan daftar langkah yang tidak mengubah apa pun. Yang TIDAK boleh dibuang: **angka, nama file, nama fungsi, dan alasan sebuah keputusan diambil**.
   - Jelaskan **kenapa**, bukan cuma **apa**. "Kuncinya dipasang pada 'ada bulan tertutup', bukan pada daftar peran" lebih berguna daripada "menambahkan pengecekan status".
   - Istilah teknis dipakai kalau memang itu namanya (`withTransaction`, 409, `FOR UPDATE`) — tapi sekali saja diberi konteks supaya orang non-teknis tetap bisa ikut.
   - Sebutkan terus terang yang **belum** terverifikasi atau yang sengaja **tidak** dikerjakan. Jangan digantung, jangan dibungkus manis.
   - Tabel dipakai kalau isinya memang berpasangan (kolom ↔ nilai, sebelum ↔ sesudah). Untuk 2-3 kalimat penjelasan, tulis paragraf biasa.


## 🎨 WAJIB — Design System

**Semua UI/UX WAJIB mengikuti `docs/design/DESIGN-SYSTEM.md`.** Tidak boleh membuat warna, tipografi, radius, spacing, atau komponen button baru di luar token yang sudah didefinisikan di sana.

### Ringkasan token kritis:
| Kebutuhan | Token | Hex |
|---|---|---|
| Primary CTA (Simpan) | `{colors.primary}` | #EF9F27 |
| Hapus / Tolak | `{colors.action-danger}` | #E24B4A |
| Setujui / Konfirmasi | `{colors.action-success}` | #1D9E75 |
| Ajukan / Review | `{colors.action-purple}` | #7C5CFC |
| Revisi / Perhatian | `{colors.action-warning}` | #BA7517 |
| Batal / Ghost | `{component.button-ghost}` | transparent + hairline |
| Canvas (dark) | `{colors.canvas-dark}` | #020F1C |
| Surface card | `{colors.surface-card}` | #042C53 |
| Teks utama (dark) | `{colors.text-primary-dark}` | #E6F1FB |

### Aturan UI wajib:
- **Font**: Inter untuk semua UI copy; monospace (`JetBrains Mono`) untuk semua angka keuangan (rupiah, kode rekening, vol, harga)
- **Radius**: 6px button/input · 10px card · 14px modal
- **Warna**: HANYA gunakan token dari design system — jangan hardcode hex baru
- **Button (WAJIB)**: Semua primary toolbar/modal/form CTA WAJIB pakai `<PrimaButton>` dari `components/ui/PrimaButton.tsx` — TIDAK BOLEH inline `<button style={...}>` atau shadcn `<Button>` untuk action utama. Variants: `primary` (Simpan), `success` (Approve/Inject/Excel), `danger` (Hapus), `purple` (Tambah/Ajukan/Form Baru), `warning` (Revisi), `ghost` (Batal/Cancel). Skip rules: row-action inline, pagination chevron, modal close X, tab pill, filter chip, shell navigation — tetap native. Spec lengkap → `docs/design/DESIGN-SYSTEM.md` section "PrimaButton"
- **Konfirmasi/notif (WAJIB)**: DILARANG native `window.confirm`/`alert()` (kotak putih browser). Konfirmasi destruktif pakai `confirmDialog()` dari `components/ui/ConfirmDialog.tsx` (`if (!(await confirmDialog({title,message,variant:'danger'}))) return`) — imperative, mount portal sendiri, tema dari `data-theme`. Pesan error/sukses pakai `toast` (sonner). Lihat **L58**.
- **Komponen shell shared**: `components/ui/UserBadge.tsx` (avatar+role+dropdown logout), `components/ui/FloatingDock.tsx` (bar nav mengambang antar-modul + aksi cepat), `components/ui/ConfirmDialog.tsx` — reuse, jangan re-implement inline.
- **Delete (WAJIB)**: Semua tombol/ikon hapus pakai `components/ui/DeleteButton.tsx` (bulat ~30px, hapus icon-only standalone di tabel) atau `components/ui/DeleteIcon.tsx` (ikon trash animasi di dalam `<PrimaButton variant="danger">`/kebab/toggle — teks & state host dipertahankan). Animasi lid-rotate + hover `#E24B4A` via rule global `button:hover .prima-del-top` di globals.css. DILARANG `<Trash2>` polos / emoji 🗑 untuk aksi hapus. Spec → `docs/design/DESIGN-SYSTEM.md` section "DeleteButton / Animated Trash".
- **Button danger**: background `#E24B4A`, teks putih — bukan outline merah
- **Jangan** pakai `financial-up/down` (#1D9E75 / #E24B4A) untuk state UI umum — itu khusus delta tabel
- **Tooltip** (WAJIB 1 standar): gradient amber dark / purple-pink light + arrow segitiga + fade 180ms. JANGAN pakai native HTML `title=""` (kotak putih browser). 2 cara: (1) CSS pseudo `::after` via `data-tooltip` attr untuk tombol di non-scroll area (ref: `.blud-act[data-tooltip]` di globals.css), (2) Portal `.blud-tip-portal` via `createPortal` untuk tombol di dalam scroll-wrapper/overflow:auto (ref: `RowActionsMenu` di dpa-client.tsx). Detail spec → `docs/design/DESIGN-SYSTEM.md` section "Tooltips — STANDAR TUNGGAL"
- Detail lengkap → `docs/design/DESIGN-SYSTEM.md`

---

## Stack
Next.js 16.2.6 · React 19 · TypeScript · MySQL 8.4 (mysql2) · JWT (jose) · Zod · Upstash Redis (atau Redis lokal) · Cloudflare Turnstile
Branch aktif: **mysql** — referensi schema: `docs/schema-mysql.sql` (bukan schema.sql/migrations — itu Postgres lama)

**Deployment**: server IT kantor (Linux/Windows Server) + PM2 + Nginx + MySQL EVENT/crontab — **bukan Vercel**. Untuk cron job, pakai MySQL EVENT (rekomendasi) atau Linux crontab → JANGAN tulis logic yg assume `vercel.json` cron.


### 🚧 Audit status — semua tahap DONE
- **Tahap 4** ✅ split `usulan-client.tsx` (-58.1%) · **Tahap 11/12** ✅ audit 5-pilar BLUD + Kinerja API (Zod sentral + getSession + bulkInsert/transaction)
- **Tahap 14** ✅ audit 5-pilar PK (`isPkRole` + Zod sentral) · **Tahap 15** ✅ Role Promotion Ladder (anti-pattern L52)
- **BLUD** ✅ Chain Hierarchy L1→L8.1 (mig 019) · Pengaturan Hapus Versi (`bludRateLimit`) · optimistic lock generic (L51, `lib/data/locks.ts` — `lib/blud/lock.ts` re-export + key khas BLUD)
- **E-Anggaran/Kinerja** ✅ Versi MURNI/PERUBAHAN (`canonical_id`, mig 020-022) · Checkpoint D drop 10 kolom turunan (mig 031) · Sumber +3 tab (mig 030) · kolom % Real Keu · bugfix unlock-latest
- **Audit V3** ✅ (2026-05-31..06-01) Auth surface + Fase A–E: V3-1 anti header-spoof `x-user-*` (L54) · V3-2 register anti-enumeration (`SIGNUP_BLOCKED`) · V3-3 kuota `AKTIF` · V3-4 kuota enforce di verify-email (`FOR UPDATE`) · V3-5 lockout login atomik (L55) · V3-6 Kinerja SSK/Realisasi optimistic-lock (L51 reuse `blud_locks`, live-verified). Fase A race (harness `scripts/concurrency-test.js` 9/9) · B state-machine · C/D/E 0 defect

## File Kritis
- `proxy.ts` — Edge Runtime, route guard, role access, CSP nonce — **JANGAN buat middleware.ts** (konflik fatal, error saat dev)
- `lib/data/db.ts` — MySQL pool, tagged template `sql\`\``, plus `withTransaction({tx})`, `bulkInsert`, `sqlInt`, `safeInt`
- `lib/security/auth.ts` — JWT, session, bcrypt; throw kalau JWT_SECRET kosong; cek revocation di getSession
- `lib/security/auditlog.ts` — audit trail semua aksi kritis (silent fail, JANGAN hapus try/catch)
- `lib/services/notifications.ts` — `addNotif()`, `sanitizeNotif()`, `buildNotifRecipients()`
- `lib/constants.ts` — roles, BIDANG_TO_SUBBIDANG, SUBBIDANG_TO_BIDANG
- `next.config.ts` — HTTP security headers (X-Frame-Options, HSTS, nosniff, dll) — **JANGAN hapus/lemahkan `securityHeaders`**; CSP dikelola di `proxy.ts` (nonce per-request)
- `.github/workflows/security-scan.yml` — **6 gate CI** (push/PR ke mysql/main): **A** Semgrep SAST · **B** npm-audit (block HIGH) · **C** tsc+ESLint · **D** Gitleaks secret-scan (`.gitleaks.toml`, allowlist `docs/` = contoh/placeholder, kode+`.env` tetap dipindai) · **E** token warna ratchet (`npm run check:tokens`, baseline `docs/design/token-baseline.json`) · **F** glyph PDF WinAnsi (`npm run check:pdf`, ketat tanpa baseline — **L71**) · **G** sakelar maintenance menutup API (`npm run check:killswitch`, statis 65 route/6 modul — **L72**). CI pakai **Node 20 / npm 10.8.2** → `package-lock.json` WAJIB regen dgn npm versi sama (`npx npm@10.8.2 install`) biar `npm ci` tak EUSAGE (**L57**)
- `package.json` — `overrides` (`postcss`/`uuid`) untuk patch CVE transitif tanpa downgrade `next`/`exceljs`; JANGAN `npm audit fix --force` (**L56**)
- `app/(dashboard)/usulan-kebutuhan/usulan-client.tsx` — modul usulan (client)
- `app/api/usulan/` — CRUD, bidang, telaah, putusan, export
- `app/api/rima/query/` + `app/api/rima/summary/` + `lib/rima/registry.ts` — Rima Q&A baca-data (F6a, GET read-only): registry **7 provider** (`usulan/bba/pk/lkjip/blud/kinerja/rencana_aksi`, masing-masing 1 file `*-provider.ts`, reuse guard modul G31). Intent: `rekap/lookup/top/tren/inbox`. `/summary` = "Tugasku" lintas-modul (agregasi `provider.inbox`). Ownership dari role server (L60/G20), guard modul (L61), Zod intent+field allowlist (G24/G25), anti-scraping (G26), kill-switch fail-closed `app_status_rima_query` (G30), audit `RIMA_QUERY`/`RIMA_QUERY_ABUSE`. **Tambah modul = 1 provider + 1 baris registry** (DoD: §24.5 CONCEPT). Fail-log: `app/api/rima/feedback/` (#2) + panel admin "RIMA FEEDBACK" (SA-only). Klien: `lib/sentinel/data-query.ts`.
- `app/(auth)/reset-password/page.tsx` — UI form reset password (consumer dari email link)
- `app/api/auth/reset-password/route.ts` — POST consumer token (hashed)
- `app/(dashboard)/blud/` — modul BLUD (hub, DPA, Pergeseran)
- `app/api/blud/` — API DPA & Pergeseran DPA
- **Impor DPA berhenti di form** (2026-08-27, **L78**): `app/api/blud/dpa/import` kini **pembaca berkas saja** — `step=commit` dibuang, dijawab 410 `IMPOR_TIDAK_MENULIS` utk tab lama. Sebabnya: `ImportDpaModal` dulu memegang `versiTanggal` sendiri (bawaan `tanggalHariIniWIB()`) **dan** jalur tulisnya sendiri, jadi memilih "Periode Juli" lalu mengimpor menghasilkan versi **Agustus** — dan karena simpan itu hapus-lalu-tulis-ulang per `(tahun, versi_tanggal)`, versi bulan berjalan yang sudah berisi bisa tertimpa (ambang keselamatan cuma menyalak kalau baris **turun** drastis; 558 → 558 lolos). Yang dibuang jalur tulisnya, **bukan** tanggal bawaannya disamakan: dua pintu tulis = dua set aturan, dan itu sudah melahirkan lubang nyata — `entri_historis` ada di kedua skema tapi rute impor memanggil `saveDpa` dgn 7 argumen, jadi dokumen historis yang **ditolak** lewat Simpan justru **diterima** lewat Impor (L69 lewat pintu lain). `DpaImportBodySchema` ikut dibuang. Penggantinya `asal_impor` (berkas/lembar/jumlah baris) di body Simpan — pola persis `asal_salin`/`asal_pulihkan`, tanpa kolom DB, hidup hanya di detail `BLUD_SAVE_DPA`; WAJIB dilepas di tiap jalur yang mengganti baris lewat cara lain, kalau tidak auditnya berbohong. Pelengkapnya `gantiPeriode` di **dua** layar (**L78b**): periode historis → `setRows([])`+`setVersi('')`+`setVersion(0)`; balik ke bulan berjalan → muat ulang versi terbaru, bukan layar kosong. Plus konfirmasi sebelum membuang isian belum tersimpan (patokannya kini `belumTersimpan`, lihat L79b). Regresi: `npx tsx scripts/test-blud-impor-periode.mts` (44) — 5 uji mutasi tertangkap; pemeriksaan "tidak boleh ada lagi" **membuang komentar dulu** supaya prosa yang menjelaskan bug lama tidak menyalakkan tesnya sendiri
- **Periode terpilih tidak lenyap sesudah Simpan** (2026-08-28, **L79**): sesudah berhasil menyimpan ke Periode Juli, pemilih periode melompat balik ke "PERIODE BULAN BERJALAN" — terlihat seperti tulisan Juli ditimpa Agustus, dan lebih buruk dari terlihat: koreksi kedua (wajib, karena hasil impor sering perlu dibetulkan) **benar-benar** mendarat di bulan berjalan. Akarnya sama dgn L78b lewat pintu lain: pemilih periode & versi yang dibuka boleh menunjuk tempat berbeda. **Empat pintu** ke sana, tiga di antaranya dulu tidak menyentuh `periodeTulis` sama sekali: `gantiPeriode` · `bukaVersi` (daftar versi) · `pulihkanSimpanan` · cabang `json.ok` Simpan. Keempatnya kini lewat `periodeUntukVersi()` di `lib/blud/tanggal.ts` — dan aturannya **bukan** "bukan hari ini berarti kunci", melainkan `tanggalPeriodeHistoris()`: hanya **akhir bulan yang sudah lewat** yang mengunci. Revisi harian (mis. 2026-08-27) sengaja memulangkan `''`; kalau ia ikut mengunci, membuka revisi kemarin lalu Simpan akan **menimpa** revisi kemarin dan riwayat harian berhenti tumbuh. `PeriodeVersiSelect` menyisipkan balik periode yang sedang dipilih walau bulannya sudah punya versi (`periodeHistorisTersedia` memang hanya menawarkan bulan kosong) — tanpa itu pemicunya jatuh ke "bulan berjalan" sementara Simpan tetap menulis ke Juli. **L79b**: `belumTersimpan` state baru, karena `versi` kosong melewatkan dua kasus nyata — sesudah Pulihkan `versi` terisi padahal isinya belum tersimpan, dan menyunting satu sel tidak terdeteksi sama sekali; jalur suntingnya dijepit satu pintu (`ubahRows` = `onChange` tabel). **L79c**: Form Baru/Impor mati saat versi tersimpan terbuka (`alasanKunciBorongan`, sekaligus jadi `data-tooltip` — tombol mati tanpa sebab itu UX buruk); Salin Tahun patokannya `history.length > 0` (**tahun tujuan**, bukan slot versi — memilih periode historis mengosongkan layar tapi tahunnya tetap berisi); "Sinkronkan DPA" & "Salin ke Induk" **sengaja tidak ikut** — keduanya tidak mengganti baris. 409 `VERSION_CONFLICT` berhenti menuduh "orang lain" saat `expected === 0` (versi baru yang ternyata sudah ada = paling sering simpanan sendiri). Pengingat belum-tersimpan: `lib/shared/belum-tersimpan.ts` — 3 pintu keluar, 3 mekanisme (`beforeunload` utk muat ulang/tutup/Keluar · klik `<a>` dicegat di fase **capture** karena `beforeunload` tidak berbunyi utk navigasi App Router · `bolehTinggalkanHalaman()` utk tombol shell yg `router.push`, dan pada Keluar dipanggil **sebelum** sesi dimatikan). Bilah gulir `.versi-menu` berkedip krn `overflow-x: visible` otomatis jadi `auto` saat `overflow-y: auto` → diberi `overflow-x: hidden` + ruang 4px kiri-kanan di varian brutalist. **Yang HILANG, sengaja**: "buka arsip periode lalu simpan sebagai revisi hari ini" — itu memindahkan pagu setahun ke angka bulan lampau; penggantinya Tahap 3 "Salin dari versi lain". **L79d** (susulan hari yg sama): `periodeHistorisTersedia` cuma menawarkan bulan KOSONG — benar utk memilih, salah utk KEMBALI, karena begitu Juli tersimpan ia lenyap dari pemilih periode dan satu-satunya jalan pulang lewat daftar versi (dua tempat, satu maksud). Bulan berarsip kini ikut ditawarkan, disaring `tanggalPeriodeHistoris` supaya revisi harian yg kebetulan lahir di bulan itu (mis. 26 Jul) TIDAK ikut — menyimpannya tidak mendarat di 31 Jul, jadi tempatnya memang di daftar versi. Memilihnya `bukaVersi`, bukan mengosongkan layar: layar kosong dgn sasaran Simpan yg sudah berisi itu jebakan. Kalimat `BludHistorisJadiPaguError` ditulis ulang **dua kali**: pemendekan saja tidak cukup, "ia akan jadi acuan pagu setahun" dilaporkan tetap tidak terbaca. Bentuk final dibuka dgn **tombol yang harus ditekan**, dan kata-katanya sama dgn yg tertulis di layar tiap modul — DPA "Impor atau buat", Pergeseran "Buat Pergeseran" (menyebut Impor di layar Pergeseran menyuruh orang mencari tombol yg tidak ada). Sebabnya tetap disebut sekali, tapi pakai kata sehari-hari: "kalau tidak, angka Juli 2026 yang dipakai jadi pagu tahun ini". Diuji **sungguhan** (bukan dicocokkan ke sumber) — mengimpor `data.ts` dari skrip tes aman, `createPool` mysql2 tidak menyambung sampai kueri pertama dan seluruh env-nya bernilai cadangan. Regresi: `npx tsx scripts/test-blud-periode-terkunci.mts` (84) — 6 uji mutasi tertangkap; jendela pemeriksaan `gantiPeriode` di suite impor-periode kini **membuang komentar dulu**, kalau tidak satu paragraf penjelasan baru menggeser kode yg diperiksa ke luar jendela
- **Pratinjau Serapan** (2026-08-28, Tahap 2): `lib/blud/pratinjau-serapan.ts` + `components/blud/PratinjauSerapanModal.tsx` — modal di layar Realisasi. Realisasi diketik SATU PER SATU (tidak ada jalur impor transaksi) dan `kunciDanPeriksaPagu` menolak per transaksi, jadi rekening yang plafonnya kurang baru ketahuan di transaksi ke sekian. Di sini orang mengetik rencana belanja (mis. total Jan–Agu dari catatan manual) lalu langsung lihat rekening mana yang akan ditolak & kurang berapa. **Murni pembacaan**: nol endpoint, nol kolom, nol migrasi — barisnya dipinjam dari `rows` layar Realisasi yang sudah dimuat, jadi nol pagar akses & nol permukaan sakelar baru (L72). Isian tidak disimpan ke mana pun. **Dua jebakan yang dihindari**: (a) dasar hitungnya `terserap` SETAHUN, BUKAN kolom `sisa` layar Realisasi — `sisa` mengikuti bulan terpilih sedangkan pagar server menjumlah setahun tanpa saringan bulan, jadi memakainya melaporkan sisa lebih longgar dari kenyataan (tipe `BarisPratinjau` sengaja TIDAK menerima `sisa`); (b) urutan "paling mepet" berlapis tiga — `sisaSetelah` menaik polos mengangkat ratusan rekening berpagu NOL ke puncak dan mengubur baris yang perlu dilihat. Rumus `kurang = terserap + rencana − pagu` HARUS sama dengan `BludPaguTerlampauiError.kekurangan`; kalau server berubah, layar ini wajib ikut. Hitungannya dipisah dari JSX supaya bisa diuji sungguhan. Regresi: `npx tsx scripts/test-blud-pratinjau-serapan.mts` (31) — 5 uji mutasi tertangkap, termasuk `.filter(Boolean)` yang membuang baris kosong pemisah daftar (ketahuan saat dicoba di aplikasi, bukan dari kode)
- **Salin dari Versi Lain** (2026-08-28, Tahap 3, **L80**): `lib/blud/salin-versi.ts` + `components/blud/SalinVersiModal.tsx` — mengembalikan kebutuhan yang ikut hilang saat L79d menutup "buka arsip Juli lalu Simpan jadi revisi hari ini": memulai versi baru dari angka versi lain. Aturannya satu, dan seluruh rancangannya berdiri di atasnya: **menyalin mengganti ISI layar, TIDAK PERNAH sasaran Simpan**. `terapkanSalinVersi` di kedua layar sengaja tidak memanggil `setVersi`/`setPeriodeTulis`/`setVersion`, dan modalnya tidak punya pemilih sasaran — sasaran ditampilkan sebagai keterangan. Modal yang memegang tanggal tulisnya sendiri persis bentuk L78 dan sudah pernah menimpa 558 baris bulan berjalan. Efek sampingnya bagus: `versi` yang tetap terisi membuat `expectedVersionUntuk` memulangkan angka kunci yang BENAR (menimpa versi terbuka memang harus membawa angka kuncinya, bukan 0). Rumus sasaran jadi satu — `sasaranSimpan(periodeTulis)` di `tanggal.ts`, dipakai tombol Simpan DAN modal; dua salinan rumus yang sama adalah cara L78 lahir. Beda dgn Salin Tahun cuma satu kolom: `anggaran_key` di sana **dilepas** (tahun beda), di sini **utuh** (tahun sama) — jadi memakai `dpaKeInput`/`pergeseranKeInput` apa adanya, TANPA mapper ketiga (kolom yang lupa didaftar terbuang senyap, dan jangkar itu yang seluruh fitur ini menjaganya). **Sengaja di luar `alasanKunciBorongan`**: tombol-tombol itu dikunci karena membawa baris dari LUAR tahun ini dgn jangkar kosong; di sini sasaran yang sudah berisi justru pemakaian utamanya. Penggantinya: sumber tidak boleh menunjuk versi yang **dibuka** maupun **sasaran** (dua pengecualian berbeda — sesudah Form Baru/ganti tahun `versi` kosong sementara sasaran tetap hari ini). Khas Pergeseran: `dpaVersi` IKUT pindah (baris pergeseran membawa salinan kolom DPA-nya; tanpa ini tabelnya mengaku mengacu DPA lain dan `PERGESERAN_TIDAK_BERIMBANG` menghitung terhadap acuan salah), dan `alasanDpaAcuanTerlaluBaru` mencerminkan pagar Zod `dpa_versi_tanggal > versi_tanggal` supaya penolakannya terbaca sebelum menekan Salin. Audit: `lingkup` pada `AsalSalinSchema` (`.default('TAHUN')` utk tab lama) — mencatat apakah **jangkarnya ikut**, satu-satunya beda yang tidak bisa disimpulkan dari jumlah baris; sengaja bukan disimpulkan dari `asal.tahun === tahun_anggaran` di route, karena jaminannya hidup di sebuah `useMemo` jauh dari sana. `PergeseranBodySchema` ikut menerima `asal_salin`. Penolong (`petakan*`, `totalAkar*`) tinggal di lib, bukan di berkas layar: identitasnya harus tetap (masuk dep pemuat modal — lahir baru tiap render = modal menembak server tanpa henti), dan fungsi di berkas `'use client'` cuma bisa dicocokkan ke teks sumbernya. `totalAkarDpa` menjumlah baris AKAR saja — terbukti perlu: pada 31 Jul 2026 akar = Rp 72,7 M sedangkan seluruh 558 baris = Rp 672 M. Nol endpoint/kolom/migrasi. Regresi: `npx tsx scripts/test-blud-salin-versi.mts` (64), 5 uji mutasi tertangkap. **BELUM diuji end-to-end**: jalur Pergeseran (2026 belum punya versi pergeseran tersimpan; yang terverifikasi keadaan kosongnya)
- **Tabel DPA & Pergeseran berhenti me-render 558 baris tiap ketikan** (2026-08-28, Tahap 4, **L81**): diukur `PerformanceObserver('longtask')` pada data asli — kotak Cari 723 ms → 0 ms, sel Vol DPA 869 ms → 77 ms, sel Vol P Pergeseran 222 ms → 0 ms (angka "sebelum" didapat dgn MENGEMBALIKAN perbaikannya lalu mengukur ulang). **Dugaan awal terbantah percobaan**: tersangkanya `validateAllPj`/`validateDupRules` yang jalan tiap render, tapi dgn seluruh baris disembunyikan lewat saringan level ketikan yang sama menghasilkan NOL long-task — biayanya di 558 × (4 combobox + menu aksi). Catatan pengukuran: percobaan pertama pakai `requestAnimationFrame` dan memulangkan ~2000 ms yang mencurigakan konsisten — itu rAF yang **dicekik ke ~1 Hz di tab tak terlihat**, bukan React; `longtask` tidak punya masalah itu. Perbaikannya **dua bagian dan keduanya wajib**: (a) `partialRecalcDpa`/`partialRecalcPergeseran` berhenti mengkloning seluruh baris — angka baru ditampung di Map dulu, hanya yang bergeser dapat objek baru (larik luar tetap baru, itu memang dibutuhkan React & tombol Simpan); (b) baris jadi komponen ber-`memo` (`DpaRow`/`PergeseranRow`) dgn TIAP prop stabil — `terpilih`/`disorot`/`isAgg` sebagai **boolean** (**L81b**), penangan dibundel jadi satu objek `aksi` ber-`useMemo`, dan yang perlu MEMBACA seluruh tabel (`addChild`/`deleteBaris`/`geser`/`handlePjChange`) lewat `rowsRef` yang diisi **di dalam efek** — bukan saat render, supaya ref memegang nilai yang sudah commit (semantik yang persis sama dgn closure yang digantikannya). `setBlocked` di Pergeseran naik jadi konstanta modul `abaikanBlocked`: arrow inline `() => {}` lahir baru tiap render dan menyeret `geser`/`toggleCheckbox` ikut berganti identitas. Saling bergantung — mengembalikan (a) saja mengangkat DPA balik ke 869 ms, jadi memo tanpa perbaikan kloning tidak menggigit sama sekali. **Virtualisasi sengaja DITOLAK** walau `react-virtuoso` sudah jadi dependensi: 4 tempat memanggil `getElementById('dpa-row-…')` (lompat dari pencarian, spanduk konflik PJ, spanduk baris kembar, baris baru) dan `useSentinelFeed` menerbitkan `rowDomIdPrefix` — baris di luar layar tidak ada di DOM, jadi semuanya diam-diam berhenti bekerja; memoisasi jangkauannya di kode dan mempertahankan perilaku apa adanya. Bahaya yang ikut diuji: `rowsRef` basi akan MENGHILANGKAN suntingan sebelumnya — tiga sel disunting dalam SATU task tanpa jeda (lebih kejam dari mengetik sungguhan, karena event diskret membuat React membilas efeknya lebih dulu), ketiganya bertahan. Regresi: `npx tsx scripts/test-blud-tabel-cepat.mts` (53) — bagian A–C menguji PERILAKU sungguhan dgn `recalcDpaJumlah`/`recalcPergeseranJumlah` sebagai pembanding; 5 uji mutasi tertangkap
- **Tutup Pergeseran** (2026-08-29, **L82**, konsep `docs/CONCEPT-blud-tutup-pergeseran.md`): `lib/blud/tutup-pergeseran.ts` + `lib/blud/tutup-data.ts` + `lib/blud/sinkron-dpa.ts` + tabel `blud_pergeseran_tutup`. Setiap pergeseran dulu dibandingkan dgn **DPA murni**, jadi dokumen Februari berbunyi "menggeser 20 juta ke B" padahal itu kerjaan Januari — selisihnya campuran, dan campuran itu bahan Rekap PJ. Tutup menyalin kolom P ke kolom kiri; kolom P TIDAK disentuh, dan itu seluruh alasan pagu realisasi tak bisa bergerak (`bacaPaguTerkunci` membaca kolom `pergeseran` versi terbaru, yg dihitung dari `vol_p x harga_p`). **Berhenti di FORM** (L78/L80): mengubah isi layar + menyetel pemilih periode, yang menulis tetap tombol Simpan — nol endpoint tulis baru, jadi seluruh pagar lama berlaku otomatis DAN hak menutup = `bolehEditMenu('pergeseran')` tanpa satu guard baru (peran yg cuma bisa lihat otomatis tertutup). Ini **satu-satunya** aksi yg memindahkan sasaran Simpan — pengecualian sengaja: di Salin Versi perpindahan itu efek samping, di sini periode berikutnya ADALAH pekerjaannya (terlihat: chip periode berganti + disebut di konfirmasi). **Bulan bukan kotak** — tabelnya rentetan versi, jadi tidak ada langkah "membawa hasil Januari ke Februari"; barisnya membawa kolom kirinya sendiri ke versi berikutnya. Ini yang membubarkan tiga aturan yg sempat dirancang lalu dicabut (menerbitkan versi DPA · kolom pembeda `babak` · "bulan harus sudah lewat" — yg terakhir memblokir pemakaian sah, sebab batas penutupan itu **kebijakan** dan bisa jatuh tgl 20; karena itu penomorannya "Pergeseran ke-n", bukan nama bulan). **Dua pagar sasaran**, keduanya diperiksa pada AKIBATNYA: #1 di Zod (sasaran harus SESUDAH versi yg ditutup — Simpan itu hapus-lalu-tulis-ulang per `(tahun, versi_tanggal)`), #2 di dalam transaksi memakai `existing` yg sudah dibaca di bawah kunci (sasaran tak boleh menimpa versi yg sudah ada) dan **tidak bisa ditembus `force`** — tanpa #2, menutup Januari dgn periode tertinggal di bulan berjalan menimpa versi Agustus yg sedang jadi acuan realisasi: jumlah baris sama (ambang diam), selisih nol (berimbang lolos). Penutupan ganda ditolak **PRIMARY KEY** `(tahun_anggaran, versi_ditutup)`, bukan SELECT-dulu (L69-a); nomor putaran **tidak disimpan**, dihitung dari urutan (L55). Jejak `asal_tutup` di body Simpan (pola `asal_salin`) — satu-satunya yg tidak berhenti di baris audit, ia yg menerbitkan baris `blud_pergeseran_tutup`; WAJIB dikosongkan di SEMUA jalur yg mengganti isi tabel (6 tempat), kalau tertinggal ia mencoba menutup versi yg barisnya sudah tidak ada. **Sinkronkan DPA dua perbaikan**: (a) **L82** sadar periode — route inject selalu ambil DPA TERBARU; tombolnya memang sudah dimatikan di periode historis, jadi tak pernah menggigit lewat layar, tapi pagarnya di UI bukan di API. Kini `getDpaVersiBerlaku(tahun, versi_tanggal)` + `versi_tanggal` WAJIB di `InjectBodySchema` (bukan optional-dgn-cadangan: cadangan itu perilaku lamanya), dan tombolnya **hidup lagi** di periode historis. Utk bulan berjalan hasilnya identik — versi DPA tak mungkin bertanggal setelah hari ini. (b) **L82b** bandingkan dulu baru bertanya: sesudah penutupan SEMUA baris punya `vol_p === vol`, tanda "belum digeser" yg dipakai `injectDpaKePergeseran` — premisnya batal, seluruh tabel bisa ditarik balik ke DPA murni beserta kolom P, pagu ikut mundur, tanpa satu pengaman berbunyi. Tapi memblokirnya juga salah (kalau DPA sudah direvisi mengikuti basis, sinkron tidak mengubah apa pun). Bedanya bukan niat orangnya, tapi apakah DPA-nya cocok — dan itu BISA DIHITUNG: `bedaSinkron` per `row_id` (bukan urutan — inject menyusun ulang barisnya) & hanya UANG (uraian yg disegarkan memang tujuan tombolnya; melaporkannya menenggelamkan yg berbahaya). Nol perubahan → jalan tanpa dialog; ada perubahan → baris + nominal + spanduk merah kalau versi itu basis penutupan. Dialog "yakin?" yg dulu di depan dibuang — ia menanyakan yg sekarang bisa dijawab. **Bukan kode konfirmasi**: kode membuktikan jari sudah sengaja, bukan bahwa orangnya tahu apa yang hilang. Ditemukan hanya krn dilihat: kotak peringatan berwarna sebaris TIDAK ditimpa tema terang (merah muda di atas merah muda) → kelas `.tp-galat`/`.tp-ingat` berpasangan `[data-theme="light"]`, pola `.rl-minus-banner`. Regresi: `npx tsx scripts/test-blud-tutup-pergeseran.mts` (92), 26 uji mutasi tertangkap — salah satunya sempat **LOLOS** krn pemeriksaannya mengutip syarat sepotong sehingga `false &&` di depannya tetap cocok (**L82c**). **L82d** (susulan, laporan pemakai kedua hari itu): "Januari ikut keganti seperti Februari" — **datanya utuh** (31 Jan tetap 4 baris berselisih, 28 Feb nol; snapshot #38/#39 & kedua pagar sasaran membuktikannya), yang berbohong **pil versi**: sesudah Tutup ia tetap berbunyi "28 FEB 2026 · 558 baris" — kalimat tentang data TERSIMPAN, lengkap dgn jumlah barisnya — sementara tabel di bawahnya sudah diganti; chip periode pindah, pil tidak; toast "belum tersimpan" hilang, pil tidak. Diperbaiki **generik**, bukan khusus penutupan: `VersiDropdown` menerima `belumTersimpan` (lencana BELUM TERSIMPAN), sebab bendera yg sama sudah menyala utk sunting satu sel/Pulihkan/Salin Versi — kebohongan sama, lebih kecil; kedua layar (DPA+Pergeseran) wajib mengopernya (L69). **Mengosongkan `versi` DITOLAK**: jujur tapi melepas dua pagar — `alasanKunciBorongan` membuka "Buat Pergeseran" tepat di atas tabel hasil penutupan, dan `alasanKunciSalinVersi` berhenti mengecualikan versi yg barusan ditutup. **BERLAKU tetap berdampingan** — fakta ttg versi tersimpan, tidak berubah krn ada suntingan di layar. Code review perbaikan itu menangkap 2 cacat sendiri: tooltipnya sempat `title=` native (satu-satunya di `components/`, dilarang DESIGN-SYSTEM → `data-tooltip`, aturan CSS-nya generik jadi cukup ganti nama atribut), dan lencananya mendorong pil KELUAR LAYAR di lebar ponsel (diukur: +96px, tepi kanan 399px di viewport 375px → `@media (max-width: 520px)` melepas `· N baris`, bagian pil yg paling tidak menentukan). **L83b** (§18): versi bertanggal HARI INI tak bisa ditutup hari itu juga — `periodeSetelahTutup` memulangkan `''` → sasaran = hari ini = versi yg ditutup, dan Simpan hapus-lalu-tulis-ulang per `(tahun, versi_tanggal)` akan menimpa dokumen yg sedang ditutup. Pagarnya BENAR, kalimatnya yg cacat: "Pilih periode setelah …" menyuruh hal yg tak bisa dilakukan (`periodeHistorisTersedia` batasnya bulan ini − 1, tanggal sesudah hari ini tak pernah ditawarkan) — cacat yg sama dgn penolakan sasaran-dihuni. **Membidik besok DITOLAK**: `periodeUntukVersi` memulangkan `''` utk tanggal bukan-akhir-bulan-lampau, jadi sesudah Simpan ke besok pemilih periode pulang ke "bulan berjalan" dan koreksi berikutnya mendarat di hari ini — menimpa versi yg barusan ditutup (L79 lahir kembali, lebih parah). Yang berlaku: tutup **besok atau sesudahnya**, cocok dgn pemakaian nyata (putaran ditutup SESUDAH selesai). Kalimatnya disusun 3 bagian (masalah/sebab/jalan keluar) + `.tp-galat` ber-`white-space: pre-line` — draf pertama ditolak pemilik aplikasi krn kaku ("bertanggal", "butuh tanggal sendiri", "mendarat", "selisihnya" bukan kata orang kantor)
- **Kunci setahun jalur versi** (2026-08-29, **L84**, konsep `docs/CONCEPT-blud-realisasi.md` §5.3.1): `BLUD_VERSI_ENTITY` + `bludTahunKey` di `lib/blud/lock.ts`, dibungkus `kunciVersiTahun(tx, tahun)` di `data.ts`. `pagarHapusVersi` membuka dgn "versi ini `MAX(versi_tanggal)` atau bukan?" — kalau bukan, hapus dibiarkan lewat (BENAR: pagu tak bergeser). Cacatnya **kapan** dibaca: sebelum kunci apa pun, jadi jawabannya dari snapshot yg bisa usang. Dua penghapusan berbeda di tahun sama saling melewatkan pemeriksaan → T1 hapus Des (penerus Jun 5jt ≥ terserap 3jt, lolos) × T2 hapus Jun (MAX masih Des, lolos tanpa periksa) → tersisa Jan 1jt, **pagu di bawah realisasi**. Kunci diambil sbg perintah **PERTAMA** di **6** transaksi (saveDpa ×2, savePergeseran ×2, 2 jalur hapus — L69: jalur simpan ikut, sebab menyimpan versi lama yg dijawab "bukan sumber pagu" lalu menghapus yg terbaru menghasilkan keadaan sama). Pertama, bukan di tengah: snapshot baca-konsisten lahir di `SELECT` BIASA pertama, ambil belakangan = menjaga jawaban yg sudah salah (L55); `assertBludVersion` boleh mendahului krn locking read. **Entity sendiri, bukan `BLUD_PERIODE_ENTITY`** — menumpang entity Tutup Kas membentuk siklus buntu: hapus-versi (periode-tahun → pagu) × Tutup Kas (periode-tahun → baris bulan) × catat BKU (baris bulan → pagu); entity terpisah membuatnya selalu terluar. Regresi: `node scripts/test-blud-race-hapus-versi.mjs` (7, DB sungguhan & deterministik — memutar balapannya 2× tanpa/dengan kunci, tahun uji 2099, auto-bersih) + `npx tsx scripts/test-blud-kunci-versi.mts` (10 statis, 5 uji mutasi tertangkap)
- `lib/blud/data.ts` + `lib/blud/recalc.ts` — kalkulasi hierarki DPA BLUD
- **Cadangan JSON BLUD ke Drive** (2026-08-31, konsep `docs/CONCEPT-blud-cadangan-json.md`): `lib/blud/cadangan-berkas.ts` (bentuk berkas + pembaca, **bebas dependensi server** krn dipakai komponen `use client`) + `lib/blud/cadangan-json.ts` (DB + Drive) + kolom `blud_riwayat_simpan.drive_file_id` (mig `migration-blud-riwayat-drive.sql`, cermin `lkjip_versi`). Yang diunggah foto per-simpan yg SUDAH tersimpan, **bukan** ditempel ke tombol Simpan — kalau ditempel, satu panggilan Google yg lambat menahan kunci setahun (L84). Penanda ditulis SESUDAH unggahan berhasil; terbalik = foto mengaku tercadang padahal Drive kosong. Kegagalan per berkas dicatat, tidak dilempar. **Satu logika, dua pemicu**: `cadangkanJsonBlud()` dipanggil route cron (`verifyCronSecret`) DAN tombol di layar Pengaturan — dua salinan = L78. Cron SENGAJA tak memeriksa sakelar maintenance (sakelar menutup pintu PEMAKAI; mencadangkan tetap diinginkan) & di luar `app/api/blud` jadi tak dipindai gate G. Izin `BLUD_CADANGAN_ROLES` sendiri, bukan menumpang `canHapusVersi`. Yang terpenting di panelnya BUKAN tombolnya melainkan baris **"Terakhir berhasil"** — cadangan DB di server ini berhenti 2 bulan tanpa ada yg sadar krn tanda hidupnya cuma di berkas log; cron pun mencatat audit SELALU, termasuk saat nol berkas naik. **Tahap 3 "Muat dari Berkas"** (`components/blud/MuatBerkasButton.tsx`, dua layar): berhenti di FORM (L78/L80) — nol endpoint tulis baru, mengganti ISI bukan sasaran, tunduk `alasanKunciBorongan`. Berkas dari luar = masukan tak tepercaya: Zod + tolak `tahun_anggaran`/`jenis` yg tak cocok (baris membawa `anggaran_key`, jangkar terikat tahun). Jejak `asal_berkas` WAJIB diputuskan di SETIAP jalur pengganti baris — suite membandingkannya dgn `asalSalinRef`, dan itulah yg menemukan Pulihkan melewatkannya. **Susulan (§8)**: tombolnya SALAH NAMA dan SALAH KUNCI. `alasanKunciBorongan` utk tombol yg membawa baris dari LUAR dgn jangkar KOSONG; berkas cadangan membawa `anggaran_key` lengkap (558/558) jadi sifatnya = Salin Versi yg memang sengaja di luar kunci itu (L80) — kuncinya dilepas, sebab keadaan yg paling membutuhkannya justru satu-satunya yg kebetulan terbuka. Guard "isi hasil hitungan" DITOLAK: menjaga dari salah pencet bukan salah sasaran (L76), dan tombolnya tidak menulis apa pun — gesekan berat di aksi tak merusak melatih orang menembusnya. Yg diperbaiki LABEL-nya: "Muat dari Berkas" -> **"Pulihkan Cadangan"**, digeser dari sebelah Impor (MENYUSUN) ke sebelah dropdown versi (MENGEMBALIKAN), + kalimat dialog yg menyebut tombol yg MEMANG ada di layar itu (L79d). Env `GOOGLE_DRIVE_FOLDER_ID_BLUD_JSON` (folder TERPISAH dari dump DB: ini polos, itu terenkripsi). Regresi: `npx tsx scripts/test-blud-cadangan-json.mts` (48), 14 uji mutasi tertangkap
- `lib/blud/riwayat-simpan.ts` + `app/api/blud/riwayat-simpan/` — riwayat simpan per jam (snapshot tiap klik Simpan, retensi 50). `toDateStr` PINDAH ke `lib/blud/tanggal.ts` supaya berkas ini tidak membentuk lingkaran modul dengan `data.ts` (yang memanggilnya); `data.ts` me-re-export agar 38 pemanggil lama tidak disentuh. Detail → catatan Database di bawah
- **Pergeseran — kolom PJ & Keterangan** (2026-08-24, `migration-pergeseran-pj.sql`): `pergeseran_dpa` kini punya `penanggung_jawab` + `keterangan`. Alasannya BUKAN kenyamanan — sebelum ini menggeser anggaran MENGHILANGKAN catatan pemilik pos, dan menu Cetak cuma bisa menyusun Rekap PJ dari DPA memakai angka SEBELUM geser (rekap yang sudah kedaluwarsa begitu terbit). Keduanya **cermin DPA, bukan isian mandiri**: `injectDpaKePergeseran` menimpanya dari baris DPA yang cocok — sederajat dgn uraian/vol/harga — dan di layar hanya baris yang lahir di situ (`row_id` `pgnew_*`) yang boleh diisi, persis aturan yang sudah berlaku utk kode_rekening & uraian. **Sengaja tanpa penjaga konflik chain** seperti DPA (`useSentinelPjGuard`): pemiliknya DPA, dan chain-conflict tetap ketahuan di panel audit menu Cetak. Cetak dapat view baru `pergeseran` × `penanggungJawab` — nominalnya `r.pergeseran` (pagu PASCA-geser), **bukan** `r.jumlah`; itu seluruh alasan view ini ada. `auditRekapPJ` dilonggarkan ke tipe struktural `AuditPjRow` supaya dipakai dua-duanya. Disimpan (bukan JOIN ke DPA saat cetak) karena `pergeseran_dpa` itu snapshot per-versi — versi DPA acuannya bisa berubah belakangan, dokumen yang sudah terbit tidak boleh ikut berubah. Jangan lupa `bolehLihatSalahSatu` di `api/blud/penanggung-jawab` sudah ditambah `'pergeseran'` (L69). Regresi: `npx tsx scripts/test-pergeseran-pj.mts` (25) — menjaga rantai SELECT→row-map→inject→cetak, terbukti menggigit lewat uji mutasi (hapus PJ dari `pergeseranKeInput` → 3 gagal; hapus timpa PJ di inject → 2 gagal)
- `components/blud/SalinTahunModal.tsx` + 2 mapper di `lib/blud/row-map.ts` — **Salin dari Tahun Lain** (konsep: `docs/CONCEPT-blud-salin-tahun.md`). Sepadan `duplicateYear` Renaksi/Kinerja tapi berhenti di **form**, bukan DB: nol endpoint baru, nol migrasi (`GET /api/blud/dpa?tahun=` & `GET /api/blud/pergeseran?tahun=` sudah cukup). Berhenti di form itu bukan kehati-hatian kosong — Sentinel jadi memeriksa salinannya SEBELUM Simpan, dan lubang kunci-ganda tidak pernah terbuka (kalau server-side, dua klik = 1.140 baris karena `FOR UPDATE` pada baris `blud_locks` yang belum ada tidak mengunci apa pun, L69-a). **Dua sumber**: `dpaKeTahunBaruInput` (pagu awal) dan `pergeseranKeTahunBaruInput` (pagu pasca-geser). Yang kedua bisa satu-lawan-satu karena `pergeseran_dpa` punya `vol_p`/`harga_p` sendiri dan `pergeseran = vol_p × harga_p` — invarian yang sama dengan `jumlah = vol × harga` di DPA, jadi `recalcDpaJumlah` server TIDAK menggeser angkanya; kalau `pergeseran` dulu angka ketikan bebas, salinan itu bakal ditimpa balik jadi vol × harga lama tanpa pesan apa pun. Mapper-nya **kebalikan `dpaKePergeseranInput` yang tepat di atasnya**: di sana `anggaran_key` sengaja dibawa (baris yang sama), di sini sengaja dibuang (tahun beda — jangkar realisasi tahun lama). `origin`/`usulan_*` juga dilepas; `row_id` apa adanya TANPA nilai cadangan (mengarang id memutus `parent_id` anak). Radio "Pasca-Pergeseran" disembunyikan kalau `peta['pergeseran'] === 'TIDAK'` — GET-nya dijaga `bolehBukaMenu('pergeseran')`, menawarkannya hanya berujung 403 (L69). Versi pergeseran belum berimbang → konfirmasi terpisah. `asal_salin` (Zod `AsalSalinSchema`) ikut body POST **semata** untuk memperpanjang detail `BLUD_SAVE_DPA`; tanpa itu tidak ada apa pun di DB yang bilang DPA 2027 lahir dari 2026. Regresi: `npx tsx scripts/test-blud-salin-tahun.mts` (30) — terbukti menggigit lewat uji mutasi (bawa jangkar → 3 gagal; salah ambil pasangan vol/harga → 6 gagal; karang `row_id` → 1 gagal)
- `lib/blud/salin-master.ts` + `components/blud/SalinMasterModal.tsx` — **Salin ke Data Induk** (konsep: `docs/CONCEPT-export-import-dpa.md` §3.9). Kebalikan `dpa-skeleton-builder.ts`: baris DPA **yang sedang di layar** → kandidat `kode_besar` + `master_akun`, menutup kasus "impor duluan, master masih kosong". Tujuan tiap baris divoting 3 sinyal (tangga `tipe_baris` · punya keturunan berkode · panjang kode **relatif** terhadap garis rekening berkas itu) + veto vol/harga; imbang jatuh ke Master Akun, tidak sepakat = tidak dicentang otomatis. **Jangan pasang ambang ruas kode mati** (`≤3 ruas`) — kode `510199` tanpa titik itu bentuk sah dan ambang itu buta padanya. Tingkat Kode Besar dipetakan lewat **nomor SLOT (L1·L2·L2.1), bukan kedalaman pohon**, dan slotnya bergeser kalau akar (`level:'L1'`) sudah ada di tabel — sebab `buildDpaRowsFromKodeBesar` menyambung L2 ke L1 lewat kecocokan kode dan MENGABAIKAN `parent_kode`, jadi **dua L2 tidak pernah bisa bersarang**. Yang tidak kebagian slot turun ke Master Akun (fakta struktural → tetap tercentang, tanpa catatan ragu; tombol "pindah ke Kode Besar" wajib disembunyikan lewat `bisaKeKodeBesar`). Menulis lewat DUA endpoint lama (`kode-besar` lalu `master-akun`) — replace-all, jadi daftar yang di-POST WAJIB dari GET yang baru saja jalan; keduanya **tidak atomik satu sama lain**, laporannya per tujuan. `kode_besar.kode` UNIQUE + batas Zod 1.000 (Master Akun tidak unik, 5.000) — dan angka itu **juga** `LIMIT` pada GET-nya, jadi daftar yang mentok di batas TIDAK boleh ditulis balik (sisanya terhapus senyap). Izin: 3 menu terpisah, dibaca dari `peta` milik `izinLayar`. Regresi: `node scripts/test-blud-salin-master.mjs` (78)
- `app/(dashboard)/perjanjian-kinerja/` — modul PK (form + 5 tab master + riwayat). Akses **per-menu** (7 menu) sejak 2026-08-03 — modul kedua setelah BLUD: `lib/pk/peran.ts` (tabel peran, modul daun) + `lib/pk/izin-server.ts` (resolusi 2 lapis) + `_izin.ts` (guard halaman) + ribbon disaring izin. `LANTAI_EDIT` = Master Pejabat (PII) & Master Unit (rename cascade) hanya SUPER_ADMIN/ADMIN, **tidak bisa ditembus matriks Admin Panel**. Konsep: `docs/CONCEPT-pk-peran.md`
- `app/api/perjanjian-kinerja/` — 13 route file + `_guard.ts` (`bolehBukaMenu`/`bolehEditMenu`/`bolehLihatSalahSatu`/`bolehModulPk`/`tolakEdit`/`tolakLantai`) — cermin `app/api/blud/_guard.ts`. Pagar baca endpoint lintas-layar (`units`, `pejabat`, `dokumen`) sengaja longgar: guard menyebut menu yang MENAMPILKAN datanya, bukan yang "memiliki"
- `lib/data/pk.ts` + `lib/data/pk-schemas.ts` — PK data layer + Zod sentral + `pkRateLimit`
- `lib/pk/docgen.ts` + `lib/pk/templates/*.docx` — Word generator (docxtemplater + escapeXml + dynamic import)
- `app/(dashboard)/buku-besar-aset/` — modul BBA (Buku Besar Aset): page + client (tabel/filter/KPI/entry/realisasi) + UserBadge + FloatingDock
- `app/api/buku-besar-aset/` — `route.ts` (CRUD list/create/update/delete), `realisasi/route.ts` (set realisasi), `_guard.ts` (`isAsetRole`)
- `lib/data/buku-besar-aset.ts` + `lib/data/buku-besar-aset-schemas.ts` — BBA data layer + Zod sentral + `bbaRateLimit`; `canonical_id` atomik (prefix `BBA-`), CAS per-row (L48 `assertCas`), validasi transisi status (`BbaTransitionError`/`BbaVersionConflictError`)
- `app/(dashboard)/lkjip/` — modul LKJIP (penyusun Laporan Kinerja tahunan): `page.tsx`+`lkjip-client.tsx` (daftar dokumen), `[id]/page.tsx`+`editor-client.tsx` (editor outline 3-panel: tree·blok·inspector + menu ⚙ Pengaturan Dokumen), `[id]/TiptapNarasi.tsx` (editor rich-text Tiptap, dynamic ssr:false)
- `app/api/lkjip/` — `route.ts` (list/create/delete dokumen), `[id]/route.ts` (detail/header+style_config), `[id]/finalize`, `[id]/generate` (Word — **murni download, tanpa efek samping**), `[id]/versi/route.ts` (GET list riwayat / POST simpan versi + arsip Drive best-effort), `[id]/versi/[versiId]/restore` (POST pulihkan), `section/route.ts`, `block/route.ts`, `_guard.ts` (`isLkjipRole`)
- `lib/lkjip/` — `data.ts` (CRUD+seed skeleton+CAS+guard DRAFT+validasi move pohon), `schemas.ts` (Zod sentral+role+`lkjipRateLimit`+`StyleConfig`+`sanitizeNarasiHtml`), `numbering.ts` (nomor section DIHITUNG dari pohon, tidak disimpan), `docgen.ts` (Word .docx dari nol via PizZip: styles dinamis dari `style_config` + parser HTML→OOXML utk rich-text + **Daftar Isi/Tabel/Gambar OTOMATIS via field TOC + caption SEQ** + footer/nomor halaman + penomoran romawi-depan/arab-isi multi-section), `versi.ts` (hybrid riwayat: snapshot JSON→DB utk pulihkan/edit-ulang + retention 20). Editor narasi = **Tiptap** (MIT). Arsip docx→Drive via `lib/services/drive.ts` (env `GOOGLE_DRIVE_FOLDER_ID_LKJIP`, best-effort, saat Simpan Versi). Fase 1+formatting+riwayat selesai; PDF (LibreOffice) & composer auto-pull = fase lanjut

- `app/api/rencana-aksi/import/` — **Import matriks Renaksi** dari file, 2 langkah: `?step=preview` (multipart, parse-only, TIDAK tulis DB) → `?step=commit` (JSON baris hasil konfirmasi user). Terbuka untuk **semua pemegang akses modul** (guard saja, tanpa pagar peran tambahan) — termasuk mode `ganti`, karena hapus baris satu per satu memang sudah boleh dan `ganti` tetap tunduk Kunci Periode. `lib/renaksi/grid.ts` (pembaca .xlsx exceljs / .csv / .pdf **digital** via pdfjs — teks+koordinat direkonstruksi jadi tabel, batas kolom dari histogram frekuensi x; PDF hasil scan DITOLAK, bukan OCR) + `lib/renaksi/import-renaksi.ts` (deteksi kolom: alias header sadar-merge > content voting yang MEMVERIFIKASI isi tiap kolom skalar; deteksi level 3 sinyal voting: kolom entitas terisi · segmen kode `X.XX.01`=program/`+1.10`=kegiatan/`+0001`=sub · kata kunci; hierarki induk via stack urutan baris; baris "indikator ke-(n)" tanpa entitas di-anchor ke pemilik lewat penomoran `(n-1)`; **kolom bulanan berpola "nilai TW diulang 3×" dikenali → simpan target TW saja, jangan `bulan_target`**, kalau tidak nilai bulanan asli disimpan) + `lib/renaksi/import-data.ts` (commit `withTransaction` + INSERT…ON DUPLICATE KEY UPDATE pada `uk_tahun_level_ind`; **kolom realisasi sengaja tidak ikut di-update**; mode `ganti` hanya hapus level yang ada di file; induk wajib ada di file/DB — kalau tidak, baris DITAHAN + dilaporkan). UI: `_components/ImportRenaksiModal.tsx` (1 modal, 2 pintu: tombol Header lintas-level + tombol toolbar tiap Data Entry dgn `levelHint`) — panel pemetaan kolom, level per baris bisa dikoreksi, 3 mode tulis (default tambah+perbarui). Regresi: `node scripts/test-renaksi-import.mjs [file|folder]`. Audit: `RA_IMPORT_PREVIEW`/`RA_IMPORT_COMMIT`
- `app/(dashboard)/dashboard/` — modul **Dashboard** (ringkasan lintas-modul, READ-ONLY): `page.tsx`+`dashboard-client.tsx` (overview 5 widget: Usulan/E-Anggaran/BLUD/Renaksi/Realisasi Kinerja — KPI+mini chart) · `[modul]/page.tsx`+`detail-client.tsx` (drill-down DI DALAM app: KPI+pie+bar+tabel rekap; tombol "Lihat Detail" TIDAK ke aplikasi modul, tetap dijaga `isDashboardRole` yang sama). Akses default SUPER_ADMIN/ADMIN/KASUBAG/KABAG + grant `app_access:'dashboard'`.
- `app/api/dashboard/` — `route.ts` (GET overview) + `[modul]/route.ts` (GET detail, allowlist `isDashModule` → 404). `lib/data/dashboard.ts` (`getDashboardSummary`/`getModuleDetail` reuse data-layer tiap modul) + `lib/data/dashboard-schemas.ts` (`isDashboardRole`, `dashboardRateLimit`, Zod). app_access key: `dashboard`. app flag: `app_status_dashboard`
- `app/(dashboard)/iki/` — modul **IKI** (Indikator Kinerja Individu, konsep `docs/CONCEPT-iki.md`): `page.tsx`+`iki-client.tsx` (daftar dokumen per pejabat/tahun), `[id]/editor-client.tsx` (editor Data Pribadi + grup RHK×4 TW + modal import 2 tab Renaksi/IKI-Atasan), `_lib/types.ts` (types client). 2 varian layout: `STANDAR` (11 kolom, 2 ttd) / `DIREKTUR` (8 kolom, ttd tunggal). Kolom `jenis` MURNI/PERUBAHAN (migration-iki-jenis.sql) — PERUBAHAN hanya menambah suffix " PERUBAHAN" pada judul via `docTitle()` di layout.ts (sumber tunggal PDF+Excel+editor); UNIQUE jadi `(nip, tahun, jabatan, jenis)` — Murni+Perubahan boleh berdampingan
- `app/api/iki/` — `_guard.ts` (`isIkiRole`: SUPER_ADMIN/ADMIN + app_access `iki`), `route.ts` (list/create/delete), `[id]/route.ts` (detail + save replace-all `withTransaction`+`bulkInsert`, CAS L48), `[id]/finalize` (POST FINAL / DELETE unlock SA-only), `import-renaksi` (mapping `rencana_aksi`: jenis→aspek_b 1:1, q1..q4→target TW), `import-atasan` (kaskade RHK), `import-excel` (POST preview-only parse file Excel "VERSI BPSDMD" → `lib/iki/import-excel.ts`, dikalibrasi 20 file asli; deteksi kolom 3 lapis: manual override > jangkar baris penomoran "1..11" > header sadar-merge + content voting — verifikasi isi per kolom, laporan `columns`/`columnOptions` utk panel "Pemetaan kolom" di modal; regresi: `node scripts/test-iki-import.mjs [folder]`; tulis via create+save existing dari `ImportIkiModal.tsx` dgn pemutakhiran pejabat/atasan dari Master PK + kaskade auto-link: pilih atasan → cari dokumen IKI atasan `nip+jabatan+tahun` → `atasan_dokumen_id` + `atasan_rhk_id` per grup via match teks RHK intervensi ke `GET import-atasan`, self-link di-skip), `pejabat` (suggest dari `pk_pejabat`), `[id]/duplicate` (salin dokumen ke tahun lain — soft-FK di-NULL-kan, atasan di-resolve via NIP), `[id]/versi` + `[id]/versi/[versiId]/restore` (riwayat snapshot), `export-log` (audit IKI_DOWNLOAD — export digenerate client-side)
- `lib/data/iki.ts` + `lib/data/iki-schemas.ts` — data layer + Zod sentral (superRefine per varian) + `ikiRateLimit` · `lib/iki/layout.ts` (grid descriptor bersama PDF+Excel — 1 sumber merge/rowspan/penomoran kolom) + `lib/iki/export-pdf.ts` (jspdf-autotable) + `export-excel.ts` (exceljs) — layout WAJIB identik 5 PDF referensi. Tabel: `iki_dokumen`→`iki_rhk`→`iki_rhk_triwulan` (migration-iki.sql; UNIQUE `(nip, tahun, jabatan)` — migration-iki-nip-jabatan.sql, 1 orang boleh dokumen per jabatan utk kasus Plt.) + `iki_versi` (migration-iki-versi.sql — snapshot otomatis saat FINALIZE/UNFINALIZE, retention 20). `lib/iki/validate.ts` — validasi LUNAK target TW vs tahunan (Akumulatif=jumlah, Progres±=TW IV; Pengulangan di-skip; tidak memblokir). Banner "atasan berubah" = `atasan_stale` server-computed (banding `updated_at` di SQL). Export massal = zip client-side (PizZip, cap 100). app flag: `app_status_iki`. Audit: `IKI_*`

## Database — MySQL 8
- Placeholder: `?` bukan `$1`. Template: `sql\`SELECT ... WHERE id = ${val}\``
- `ONLY_FULL_GROUP_BY`: kolom non-aggregate wajib di GROUP BY atau pakai `ANY_VALUE()`
- `JSON_OBJECTAGG` → pakai `pool.query()` bukan prepared statement
- BIGINT (`audit_log.id`, `user_sessions.id`): mysql2 bisa return BigInt → gunakan `CAST(col AS UNSIGNED)`
- `NOW()` = waktu server MySQL. `timezone: '+07:00'` di pool hanya untuk JS Date parsing
- Kolom baru (audit Tahap 3): `usulan_headers.jumlah_item_admin`, `total_nilai_admin` — di-maintain `updateHeaderStats()`
- Kolom baru (fitur Theme Toggle): `users.theme_preference` ENUM('dark','light') DEFAULT 'dark' — migration 017
- Tabel baru (fitur Cetak BLUD): `rekap_pk` (snapshot rekap Penanggung Jawab BLUD) — migration 024. Kolom: `versi_dpa`, `label`, `nominal`, `saved_at`, `saved_by` (INT, FK users.id ON DELETE SET NULL). Pattern save: `withTransaction` DELETE+bulkInsert per versi (replace-latest)
- Tabel baru (fitur Kode Besar BLUD): `kode_besar` — migration 025+026. Kolom: `kode` (UNIQUE), `uraian`, `level` (L1/L2/L2.1, migration 026), `parent_kode` (FK soft ke kode_besar.kode, migration 026), `urutan`. Mirror master_akun pattern (replace-all atomic via withTransaction + bulkInsert). Seed 8 default rows via INSERT IGNORE (5.X/5.1/5.2/5.1.1/5.1.2/5.2.2/5.2.3/5.2.6 — standar BLUD). Fungsi: template awal saat klik "Form Baru" di DPA — overlay select per row, build hierarki L1→L2→L2.1 otomatis.
- Kolom baru Pergeseran (cermin DPA) — migration `migration-pergeseran-pj.sql`: `pergeseran_dpa.penanggung_jawab` VARCHAR(128) NULL + `keterangan` TEXT NULL. Ditulis lewat `PERGESERAN_COLUMNS` di `lib/blud/data.ts` — **tambah kolom = ubah `lib/blud/row-map.ts` juga**, itu satu-satunya tempat baris DPA/Pergeseran server→klien dipetakan (kolom yang lupa didaftar terbuang senyap lalu terkirim balik sebagai "tidak ada")
- Tabel baru (fitur Penanggung Jawab BLUD): `penanggung_jawab` — migration 027. Kolom: `label` (UNIQUE), `urutan`. Seed 13 default Kasubbag/Kasi/Kabid via INSERT IGNORE. Fungsi: master list untuk dropdown kolom Penanggung Jawab di DPA (PenanggungJawabCombobox di components/blud/). Kolom kode_rekening di DPA + Pergeseran sekarang `readOnly` — value derive dari MasterAkunCombobox pick di kolom Uraian.
- Tabel baru (fitur Buku Besar Aset/BBA, eks "Inventaris Modal"): `buku_besar_aset` — migration `migration-inventaris-modal.sql` (create) + `migration-rename-buku-besar-aset.sql` (rename tabel/flag/app_access/prefix) + `migration-bba-simplify-status.sql` (status 6→4, drop luncuran/parent_row_id, nilai_rencana auto). Register belanja modal lintas-tahun + lifecycle. Kolom kunci: `canonical_id` (identitas stabil lintas-tahun, prefix `BBA-`, UNIQUE `(canonical_id, tahun_anggaran)`), `tahun_anggaran`, `status` ENUM(DIRENCANAKAN/REALISASI_PENUH/REALISASI_SEBAGIAN/TIDAK_TEREALISASI) — **DILUNCURKAN/DIBATALKAN dihapus + infra luncuran (`parent_row_id`/`getAsetChain`) dibuang**, `sumber_anggaran` ENUM(BLUD/APBD/DAK/LAINNYA), `nilai_rencana` (**OTOMATIS = vol × harga, dihitung server di create/update — bukan input**)/`nilai_realisasi`, `version` (optimistic lock per-row — L48 CAS). Kolom turunan (sisa/pct/aging) TIDAK disimpan — dihitung server-side. App flag: `app_status_buku_besar_aset`. app_access key: `buku_besar_aset`.
- Kolom baru BBA (fitur Import dari Usulan) — migration `migration-bba-import-usulan.sql`: `origin` ENUM(MANUAL/USULAN), `usulan_item_id` (UNIQUE `uq_bba_usulan_item` — anti double-entry), `usulan_no`, `usulan_keputusan` ENUM(DISETUJUI/DITOLAK), `ditolak_oleh` ENUM(ADMIN/KASUBAG/KABAG — diturunkan dari kolom jejak putusan), `sub_bidang`, `vol_realisasi` DECIMAL(18,2) (unit terealisasi, validasi 0 ≤ vol_realisasi ≤ vol). Baris `origin=USULAN`: uraian/vol/harga/keputusan read-only; keputusan DITOLAK → realisasi terkunci (vol & nilai realisasi 0). Endpoint tarik: `app/api/buku-besar-aset/import-usulan/route.ts` (mode preview/commit, khusus ADMIN/SUPER_ADMIN).
- Tabel baru (modul LKJIP): `lkjip_dokumen` + `lkjip_section` + `lkjip_block` — migration `migration-lkjip.sql`. Penyusun Laporan Kinerja tahunan berbasis outline-tree + blok. `lkjip_dokumen`: `canonical_id` (prefix `LKJIP-`, atomik dari AUTO_INCREMENT), `tahun`, `status` ENUM(DRAFT/FINAL) (FINAL=immutable), `version` (optimistic lock dokumen-level L48 CAS). `lkjip_section`: pohon adjacency-list (`parent_id` TANPA FK — subtree delete via app `withTransaction`; `depth`/`urutan`; `locked`=1 untuk 4 BAB seed), **nomor DIHITUNG di `numbering.ts`, tidak disimpan**. `lkjip_block`: `tipe` ENUM(NARASI/TABEL/GAMBAR/GRAFIK) + `payload` JSON, FK CASCADE ke section (GRAFIK = migration `migration-lkjip-grafik.sql`; payload pie/bar/line via recharts→PNG html2canvas-pro→embed Word lewat pipeline gambar `imageFileId`; sumber data manual atau dari blok TABEL). `style_config` JSON (font/ukuran/spasi/justify+nomor halaman+footer+romawi-depan — migration `migration-lkjip-style.sql`). Tabel `lkjip_versi` (migration `migration-lkjip-versi.sql`): snapshot JSON struktur dokumen utk riwayat/pulihkan (hybrid — `drive_file_id` arsip docx di Drive, retention 20/dok, list metadata-only anti-lemot). App flag: `app_status_lkjip`. app_access key: `lkjip`.
- Kolom baru DPA BLUD (fitur Import dari Usulan) — migration `migration-dpa-import-usulan.sql`: `dpa_blud.origin` ENUM(MANUAL/USULAN), `usulan_item_id` INT NULL (non-UNIQUE — versioned snapshot, keunikan per-versi via Sentinel Guard), `usulan_no`. Tombol import per-baris di kebab DPA (L2 ke bawah) → modal **workbench** (`components/blud/ImportUsulanModal.tsx`, rewrite 2026-06-11 per `CONCEPT-import-usulan-dpa-v2.md`): 2 mode — **"Isi baris ini"** (1 item radio menimpa uraian/vol/satuan/harga baris leaf yang diklik, kode rekening/PJ/posisi dipertahankan, baris berisi → confirmDialog "Timpa") dan **"Sisip baris baru"** (multi, dock **Panel Susunan** kanan: indent/outdent ◀▶ + urutan ↑↓ eksplisit → parent resolve dari urutan susunan, bukan urutan tampil). Rail kiri jenis belanja→sub-bidang + search-first + react-virtuoso (skala ribuan item) + filter TA + sort. Styling theme-aware via kelas `.blud-imp-*` di globals.css (dark+light). Tarik item usulan final disetujui Kabag. Endpoint: `app/api/blud/dpa/import-usulan` (GET). **Sentinel Guard** anti-dobel 3 lapis: modal disable item yg sudah di form · banner live `lib/blud/dup-guard.ts` (HARD usulan_item_id kembar + HEURISTIK uraian+satuan+harga) · server 400 via `validateTreeIntegrity`.
- Kolom baru Renaksi (jangkar identitas): `rencana_aksi.kode` VARCHAR(60) NULL + `INDEX idx_tahun_level_kode` — migration `migration-renaksi-kode.sql`. Kode nomenklatur (`1.02.02.2.01`) yang sudah dibaca parser impor untuk menebak level, kini ikut DISIMPAN. Gunanya: hierarki Renaksi disambung teks nama, jadi identitas program ikut berubah kalau namanya diperbaiki — kode tidak. NULL & TIDAK unik (1 program n indikator = n baris berkode sama, seperti kolom `program`); level tujuan/sasaran memang tanpa kode. Pada upsert impor dipakai `COALESCE(VALUES(kode), kode)` supaya file tanpa kolom kode tidak MENGHAPUS kode tersimpan. Terisi dari 3 jalur: impor, isian "Kode Nomenklatur" di Data Entry (hanya level program/kegiatan/sub-kegiatan — tujuan & sasaran tidak bernomenklatur), dan `duplicateYear` (ikut tersalin ke tahun baru — di situlah jangkar ini paling dibutuhkan, karena nomenklatur biasanya berubah antar tahun). Di form kode diperlakukan seperti field lain (kirim eksplisit, kosong = hapus), BUKAN COALESCE — supaya kode bisa dihapus sengaja; syaratnya `kode` harus ikut di SELECT → RaRow → state form → payload, kalau satu mata rantai putus edit manual akan menghapus kode baris hasil impor — dijaga 5 pemeriksaan di `npm run test:renaksi-audit` (28 total; terbukti menggigit lewat uji mutasi: hapus `kode` dari SELECT → 3 gagal, pasang COALESCE di form → 1 gagal). Belum dipakai untuk deteksi ganti-nama — itu tahap berikutnya
- **Tabel baru — riwayat simpan per jam** (2026-08-26, `migration-blud-riwayat-simpan.sql`, konsep `docs/CONCEPT-blud-riwayat-simpan.md`): `blud_riwayat_simpan` — snapshot tiap klik Simpan DPA/Pergeseran. Simpan itu hapus-lalu-tulis-ulang untuk `(tahun_anggaran, versi_tanggal)` yang sama, jadi simpanan jam 16:40 MENGHAPUS hasil jam 09:15 tanpa sisa — kejadian nyata: Pergeseran 2026 disimpan 3× dalam sehari (13:34/13:55/13:59), hanya yang terakhir tersisa. Kolom: `jenis` ENUM(DPA/PERGESERAN) — **satu tabel, bukan dua kembar**, menambah jenis ketiga = 1 nilai enum + 1 pemanggilan; `disimpan_pada` DATETIME (bukan TIMESTAMP: zona sesi) distempel **server** lewat `waktuSekarangWIB()` di `lib/blud/tanggal.ts` — BUKAN `NOW()` MySQL, sebab `versi_tanggal` datang dari klien lewat `tanggalHariIniWIB()` dan pada dini hari WIB keduanya bisa beda tanggal kalau server UTC; `versi_ke` = angka kunci SESUDAH bump (= simpan ke-n hari itu); `isi` JSON = **payload POST apa adanya, bukan hasil SELECT** — supaya bisa dikirim balik tanpa penerjemahan, kalau bentuknya beda `row-map.ts` punya arah ketiga yang harus dipelihara. `versi_tanggal` **TETAP DATE**: mengubahnya jadi DATETIME menyentuh 298 kemunculan di 38 berkas DAN melonggarkan pagar `VERSI_DIRUJUK` jadi pencocokan per-detik (DPA 09:15 tak lagi terlindungi pergeseran yang menunjuk 16:40) — arah berlawanan dgn L76. Baris tabel ini **tidak dirujuk siapa pun**; pergeseran/Rekap PJ/BBA tetap menunjuk tanggal. **Ditulis di 4 jalur** (`saveDpa` ×2, `savePergeseran` ×2 — dua di antaranya cabang kosong+force yang mudah terlewat, L69) di dalam `withTransaction` yang sama, SESUDAH `bumpBludVersion`; retensi 50 per (jenis, tahun) dipangkas lewat derived table + `sqlInt` (L66). `deleteDpaVersi` **tidak** menghapusnya → versi yang salah terhapus masih bisa dipulihkan angkanya. Pulihkan = **isi form, bukan tulis DB** (nol endpoint tulis baru, semua pagar berlaku otomatis, tidak ada lubang L69-a) — angka kunci WAJIB diambil segar dari server (**L77**). Endpoint baca: `app/api/blud/riwayat-simpan/route.ts`. Audit: `BLUD_RIWAYAT_PULIHKAN` + `asal_pulihkan` di detail `BLUD_SAVE_*`. Regresi: `npx tsx scripts/test-blud-riwayat-simpan.mts` (55), 10 uji mutasi tertangkap
- **Kolom DIBUANG** (2026-08-26, `migration-drop-is-latest.sql`): `dpa_blud.is_latest` + `pergeseran_dpa.is_latest` beserta `INDEX idx_versi_latest`. Kolomnya tidak pernah ditulis — tidak ada di `DPA_COLUMNS`/`PERGESERAN_COLUMNS`, jadi SETIAP baris di SETIAP versi memakai `DEFAULT 1` dan mengaku versi terbaru (DPA 2026: 1.128 baris dari 3 versi, semuanya `1`). Tanpa gejala karena tak satu pun query memakainya dan lencana LATEST dihitung klien dari daftar riwayat — tapi `WHERE is_latest = 1` terbaca benar, jalan tanpa galat, lalu memulangkan semua versi tercampur. Dibuang, bukan dipelihara: "versi mana yang berlaku" sudah terjawab benar lewat `MAX(versi_tanggal)`, dan dua sumber kebenaran soal angka uang cepat atau lambat berbeda pendapat. Migration **aman dijalankan kapan saja** — kode berhenti membacanya lebih dulu dan query pembacanya `SELECT *`
- Untuk bulk INSERT pakai `bulkInsert(table, cols, rows[][])` — single round-trip, atomic via `withTransaction`

## Auth & Security
- Cookie: `prima_session` (HTTP-only JWT) — JANGAN ubah nama
- Fungsi kritis — JANGAN ubah: `verifyToken()` `getSession()` `setSessionCookie()`
- `proxy.ts` — DILARANG import `bcryptjs` atau `next/headers`, gunakan `jose`
- Login: max 5 attempts → lock 15 menit. Session timeout: 60 menit

## Role System
4 level: `SUPER_ADMIN / ADMIN / ADMIN_KASUBAG / ADMIN_KABAG` → `BIDANG_*` (4) → `SUB_BIDANG` (18 roles)
Detail mapping: `lib/constants.ts` → `BIDANG_TO_SUBBIDANG`, `SUBBIDANG_TO_BIDANG`

### Role Quota (max akun per role)
- `ROLE_QUOTA = 3` (existing) — berlaku untuk **6 BIDANG_ROLES** (RENBANG, UMUM, KEUANGAN, PELAYANAN, PENUNJANG, KEPERAWATAN)
- `ADMIN_QUOTA = 6` — max ADMIN (Admin Staff)
- `SUPER_ADMIN_QUOTA = 4` — max SUPER_ADMIN
- ADMIN_KASUBAG, ADMIN_KABAG, sub-bidang (18 roles) → **tidak di-cap**
- Enforcement: real-time `COUNT(*) WHERE role = ? AND status = 'AKTIF'`, BUKAN counter terpisah di DB
- Cek di entry point: register, edit role (User Management Admin Panel), promotion target

### 🔴 PENTING — 2 panel user berbeda (jangan dicampur)
| Panel | Lokasi | Peruntukan | Aksi |
|---|---|---|---|
| **"Kelola User"** | `/usulan-kebutuhan` → sidebar PENGATURAN → Kelola User | **Khusus modul Usulan** — sub-modul untuk admin Usulan ubah role user yang terlibat di flow Usulan | **Hanya ubah role**, banner explicit: "Aksi lain tersedia di Admin Panel" |
| **"User Management"** | `/admin` → tab User Management | **Sistem-wide** — full CRUD user untuk seluruh PRIMA | Nonaktif, reset password, hapus, atur akses aplikasi, ubah role, (rencana) unlock promotion lock |

**Jangan refactor / rename / merge keduanya** — peruntukan berbeda:
- "Kelola User" hidup di scope `app/(dashboard)/usulan-kebutuhan/_panels/KelolaUserPanel.tsx`
- "User Management" hidup di scope `app/(dashboard)/admin/admin-client.tsx`

## God Nodes (fungsi paling banyak dipakai — hati-hati saat ubah)
- `writeAuditLog()` · `sql()` · `hasAppAccess()` · `getSession()` — perubahan di sini berdampak luas.
